Arama barlarımda klavye her tuşa basışta kapanıyor ve yazı yazarken kesinti oluyor. Kodlarımı inceledim ve şu düzeltmeleri yapman gerekiyor:

SORUN: Bazı ekranlarda useMemo ve useCallback eksik, bazı FlatList ve ScrollView'larda keyboardShouldPersistTaps eksik, bazı TextInput'larda autoCorrect ve autoCapitalize eksik.

ŞU DOSYALARI TAM OLARAK GÜNCELLE:

1. JobListScreen.tsx:
- Import satırına ekle: import React, { useState, useCallback, useLayoutEffect, useMemo } from "react";
- filteredJobs zaten useMemo ile sarılı ✓
- handleSearch zaten useCallback ile sarılı ✓
- Ana FlatList'e ekle: keyboardShouldPersistTaps="handled" ve removeClippedSubviews={true}
- Modal içindeki TÜM ScrollView'lara ekle: keyboardShouldPersistTaps="handled"
- Ana SearchBar TextInput'a ekle: autoCorrect={false} ve autoCapitalize="none"
- Modal içindeki carrier search TextInput'a ekle: autoCorrect={false} ve autoCapitalize="none"

2. CarrierListScreen.tsx:
- Import satırına ekle: import React, { useState, useCallback, useMemo } from "react";
- filteredCarriers zaten useMemo ile sarılı ✓
- FlatList'e ekle: keyboardShouldPersistTaps="handled" ve removeClippedSubviews={true}
- SearchBar TextInput'a ekle: autoCorrect={false} ve autoCapitalize="none"

3. CompanyListScreen.tsx:
- Import satırına ekle: import React, { useState, useCallback, useMemo } from "react";
- filteredCompanies zaten useMemo ile sarılı ✓
- FlatList'e ekle: keyboardShouldPersistTaps="handled" ve removeClippedSubviews={true}
- SearchBar TextInput'a ekle: autoCorrect={false} ve autoCapitalize="none"

4. CompletedJobListScreen.tsx:
- Import satırına ekle: import React, { useState, useCallback, useLayoutEffect, useMemo } from "react";
- filteredJobs zaten useMemo ile sarılı ✓
- groupedJobs zaten useMemo ile sarılı ✓
- handleSearch zaten useCallback ile sarılı ✓
- FlatList'e ekle: keyboardShouldPersistTaps="handled" ve removeClippedSubviews={true}
- Modal içindeki ScrollView'a ekle: keyboardShouldPersistTaps="handled"
- SearchBar TextInput'a ekle: autoCorrect={false} ve autoCapitalize="none"

5. CarrierAvailabilityScreen.tsx (eğer varsa):
- Aynı mantığı uygula: useMemo, useCallback, keyboardShouldPersistTaps, autoCorrect={false}, autoCapitalize="none"

ÖNEMLİ: Her dosyada import satırını kontrol et, gerekli hook'ları ekle. Tüm TextInput'lara autoCorrect={false} ve autoCapitalize="none" ekle. Tüm FlatList ve ScrollView'lara keyboardShouldPersistTaps="handled" ekle.

Tüm bu değişiklikleri uygula ve test et.
